// import { fromJS } from 'immutable';
// import { selectStudentActiveInactiveDomain } from '../selectors';

describe('selectStudentActiveInactiveDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
