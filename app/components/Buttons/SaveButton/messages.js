/*
 * SaveButton Messages
 *
 * This contains all the text for the SaveButton component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  header: {
    id: 'app.components.SaveButton.header',
    defaultMessage: 'This is the SaveButton component !',
  },
});
